import LockOpenIcon from "@mui/icons-material/LockOpen";

export default function LoginIcon() {
  return (
    <div
      style={{
        textAlign: "center",
        margin: "20px auto",
      }}
    >
      <LockOpenIcon
        style={{
          fontSize: "50px",
          color: "#1976d2",
        }}
      />
    </div>
  );
}
